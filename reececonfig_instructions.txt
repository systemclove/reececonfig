// reece's config //

// Clean Install:
1. Right click TF2 in the steam library list and select Manage > Browse local files
2. Navigate to the "tf" folder and delete the "custom" and "cfg" folders
3. Right click TF2 in the steam library again and select Properties > General
4. Remove any launch options you already have specified and replace them with just "-autoconfig"
5. Launch the game once then close it
6. Extract the "tf" folder from reececonfig.zip into the "Team Fortress 2" folder (the folder that contains the existing "tf" folder). Allow it to replace/overwrite files if it asks to.
7. Remove "-autoconfig" from your launch options and replace them with these: (make sure to replace -w 1920 -h 1080 with whatever your monitors resolution is.)

-dxlevel 95 -windowed -noborder -w 1920 -h 1080 -console -novid -high -condebug -conclearlog -usercon -g15

8. Launch the game, the config should be installed now! look for the bunny in console to verify

// Settings:
Edit cfg/mybinds.cfg to add custom binds without disturing other configs

// Changes:
--- General ---
- Duck rebound to Q
- F1, F2, F3, F4 Bound to loadout preset swapping
- Scrolling bound to jump (for bhopping)
- Net graph can be toggled with "-"
- MegaAntiCheat (abandoned project) pre-configured
- My personal default map selection
- My hitsound and killsound

--- Medic ---
- Mouse5 bound to Ubercharge ready (team chat bind + voiceline)
- Mouse4 bound to quickbow script (hold F to fire a crossbow bolt, let go to switch back to medigun)
- 1, 2, 3 bound to Bullet - Explosion - Fire resistances

--- Engineer ---
- Mouse4 bound to build sentry
- Mouse5 bound to destroy sentry
- 4 bound to build dispenser
- E bound to build teleporter entrance
- R bount to build teleporter exit
(Note the last 3 will also destroy your old building, if you have one, as soon as you press the bind)
- B bound to teleport to spawn (eureka effect)
- Shift + B bound to teleport to teleporter exit (eureka effect)
(thanks solarlight)


//// Credits ////
SolarLight - Eureka Effect tp script
Oatmeal - offline jump script
cfg.tf - "Tweaks" folder cfg files, some parts of the other cfg files, and i believe they made the demo recording cfg

