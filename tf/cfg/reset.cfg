// reece's config //
// Reset class specific values

tf_hud_target_id_offset 0 // Reset medic heal target postition (idk if this is neccesary)
bind f +inspect // Reset quickbow bind

// Reset Vacc switcher binds
bind 1 slot1
bind 2 slot2
bind 3 slot3
bind backspace ""


// Re-enable auto reload (its disabled on spy so you can fake a reload in disguise)
cl_autoreload 1